export default (state,action)=>{
    switch(action.type){
        // case 'UPDATE_TICKERS':
        //     return{
        //         ...state,
        //         portfolio:action.payload
        //     }

        default:
            return state
    }
}
